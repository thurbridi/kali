import type { Action } from '../types/types'

const tagsReducer = (state: string[], action: Action): string[] => {
  switch (action.type) {
    default:
      return state
  }
}


export default tagsReducer